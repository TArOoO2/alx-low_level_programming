#include "main.h"

/**
* add - adds two integers and returns the result
* @a: character to test.
* @b: character to test.
* Return: return a+b
*/
int add(int a, int b)
{
	int n;
	n = a + b;
	return (n);
}
